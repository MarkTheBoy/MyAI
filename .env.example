# Environment Configuration (template)

# Server
PORT=3000

# Google Gemini API (use placeholders only)
GEMINI_API_KEYS=key1,key2,key3,key4,key5
GEMINI_MODEL=gemini-2.5-flash

# Optional
NODE_ENV=development
